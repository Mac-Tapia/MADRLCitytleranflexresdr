# requirements.txt — fuente unica de dependencias del proyecto MADRL CityLearn v3
# (HAPPO / MASAC / MATD3 / MAAC). Cubre Windows local y AWS Linux.
#
# Instalacion completa (despues de crear y activar un venv Python 3.9):
#   pip install -r requirements.txt
#   pip install --upgrade torch torchvision --index-url https://download.pytorch.org/whl/cu126
# Blackwell (RTX PRO 6000 sm_120): cu128 required — notebook cell 1.3 auto-selects.
#   pip install --upgrade torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
# El segundo comando es el unico paso separado: PyTorch necesita el indice de
# wheels CUDA especifico (cu126) para acelerar por GPU. El primer comando ya
# deja un PyTorch CPU funcional (instalado como dependencia transitiva de
# CityLearn) por si solo se necesita CPU/tests.

# ── Paquetes editables del proyecto ─────────────────────────────────────────
-e ./CityLearn
-e .

# ── Versiones fijas de compatibilidad RL (Ray/RLlib 1.8.0, MARLlib 1.0.3) ───
numpy==1.23.5
pandas>=2.0
scipy>=1.10
protobuf==3.20.3
gym==0.20.0
gymnasium==0.28.1
ray[rllib]==1.8.0

# ── Dataset y utilidades cientificas ────────────────────────────────────────
pyyaml
simplejson
scikit-learn<=1.2.2
pvlib>=0.10
requests>=2.28
tqdm>=4.65
psutil>=5.9
# Optional: richer Dunn/Conover post-hoc (uc3m.multicriteria.stats_tests falls
# back to manual implementations when this package is absent).
# scikit-posthocs>=0.7

# ── Entrenamiento, tracing y multi-agente (MARLlib/PettingZoo) ──────────────
matplotlib
tensorboardX
dm-tree
setproctitle
absl-py
icecream==2.1.3
supersuit==3.2.0
pettingzoo==1.12.0
importlib-metadata>=6.0,<9
stable-baselines3==2.2.1

# ── Documentacion ────────────────────────────────────────────────────────
sphinx==7.4.7
nbsphinx==0.9.8

# ── Tests ────────────────────────────────────────────────────────────────
pytest>=7.0
pytest-cov>=4.0
